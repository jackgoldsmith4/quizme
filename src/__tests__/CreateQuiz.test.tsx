import * as React from 'react';
import { render, fireEvent, waitForElement } from "@testing-library/react";

import CreateQuiz from '../components/CreateQuiz';

describe("<CreateQuiz />", () => {
    test("display create-quiz page", async () => {
        //TODO
    });
});