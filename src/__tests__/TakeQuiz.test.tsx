import * as React from 'react';
import { render, fireEvent, waitForElement } from "@testing-library/react";

import TakeQuiz from '../components/TakeQuiz';

describe("<TakeQuiz />", () => {
    test("display take-quiz page", async () => {
        //TODO
    });
});